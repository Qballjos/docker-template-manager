name: CI/CD Pipeline

# This workflow is temporarily disabled
# It will be re-enabled when dependencies are properly set up
# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# No jobs defined - workflow is completely disabled