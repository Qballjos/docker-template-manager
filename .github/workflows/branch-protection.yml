name: Branch Protection

# This workflow is temporarily disabled
# It will be re-enabled when branch protection rules are properly configured
# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# No jobs defined - workflow is completely disabled
