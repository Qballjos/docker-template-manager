# Docker Template Manager - Unraid Configuration
# 
# This file is for reference only. In Unraid, configure these
# through the Docker container template settings.

# ============================================================
# REQUIRED
# ============================================================

# API Key - Authentication for WebUI access
# Leave empty in Unraid to auto-generate (recommended)
# Or generate: openssl rand -base64 32
API_KEY=

# ============================================================
# OPTIONAL (Unraid sets these automatically)
# ============================================================

# CORS Allowed Origins - Auto-set to your Unraid IP
ALLOWED_ORIGINS=http://192.168.1.100:8080

# Debug Mode - MUST be false for security
FLASK_DEBUG=false

# Your Timezone
TZ=America/New_York

# Directory Paths (set by Unraid template)
TEMPLATE_DIR=/templates
BACKUP_DIR=/backups
CONFIG_DIR=/config

# Backup Retention
BACKUP_RETENTION_DAYS=30
